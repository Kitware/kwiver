KWIVER 2.4.0 Release Notes
=================================

This is a minor release of KWIVER that provides new functionality and fixes over
the previous v2.3.0 release,

Updates
-------

Arrows: OCV

* Add CLAHE image_filter.
* Add histogram equalization image_filter.

Arrows: VTK

* Added the following implementations of the mesh_filter algo:
  smooth_mesh_filter, decimate_mesh_filter, triangulate_mesh_filter.

* Added VTK mesh_container implementation.

Arrows: Ceres

* num_distortion_params() now returns size_t.

Arrows: Core

* Added texture_mesh algorithm implementation.

* All algorithms taking unsigned int configuration parameters now take size_t.

* Moved Obstacle Telesculptor fork UV unwrap mesh behavior into
  core/algo/uv_unwrap_mesh, with legacy behavior available using
  sort_descending=true and compact=false.

* Corrected match_features_fundamental_matrix logger name.

Arrows: FFmpeg

* Add compatibility for FFmpeg v8.0 API.

Arrows: MVG

* The bundle-adjust-tool applet now accepts --focal-length to override the focal
  length of all input cameras before optimization.

* The init-cameras-landmarks applet now accepts --cam-in, a list file of
  KRTD camera paths, as an alternative to initializing cameras from metadata.

* The bundle-adjust-tool applet now reports reprojection RMSE over all tracks
  (in addition to trusted tracks only) before and after bundle adjustment.

* All algorithms taking unsigned int configuration parameters now take size_t.

Python

* Added bindings for video_output and video_settings.
* No building wheels for or testing against python version 3.8

Track Oracle

* Removed track_oracle.

Vital: Algo

* Added mesh_filter algorithm interface.

* Added texture_mesh algorithm interface.

* Removed video_input.metadata_map().

* No longer require timestamp references in video_input's next_frame()
  and seek_frame().

* compute_depth.compute() now uses size_t for the reference_frame parameter.

* filter_features.filter() now uses size_t for the indices parameter.

* resection_camera.resection() now uses size_t for the height and width parameters.

* Removed video_input.seekable().

* Added video_input.seek_time().

* Replaced video_input.IS_SEEKABLE with video_input.IS_SEEKABLE_BY_FRAME
  and video_input.IS_SEEKABLE_BY_TIME.

* Used shared_ptr instead of unique_ptr for video_settings returned by
  video_input.implementation_settings().

Vital: IO

* intrinsics_from_metadata() now uses size_t for the height and width parameters.

Vital: Types

* Added mesh_container and simple_mesh_container types for passing mesh data
  between algorithms.

* Removed local_geo_cs.

* Removed local_cartesian.

* Removed test_equal_content().

* Added width, height, and frame rate accessors to video_settings.

* camera now uses size_t for image_height() and image_width().

* camera_intrinsics now uses size_t for image_height() and image_width().

* covariance_ now uses size_t for indexing.

* image_container now uses size_t for get_image().

* landmark_ now uses size_t for observations().

* Unified platform and sensor yaw/pitch/roll metadata items into orientations
  using vital::rotation_d.

* Metadata now holds absolute instead of relative sensor orientation.

Building:

* Kwiver now supports using Spack to manage third party dependences.

Bug Fixes
---------

* The rotation_ equality operator now operates more correctly.

* Fix failure to find Ceres v2 headers.
